import { PoliticPipePipe } from './politic-pipe.pipe';

describe('PoliticPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PoliticPipePipe();
    expect(pipe).toBeTruthy();
  });
});
